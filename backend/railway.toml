[build]
builder = "nixpacks"

[deploy]
startCommand = "npx prisma generate && npx prisma db push --accept-data-loss && node src/server.js"
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3

[[services]]
name = "acapulco-api"
